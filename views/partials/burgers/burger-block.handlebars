{{!-- <li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newDevour="{{devoured}}">
		{{#if devoured}}DEVOURED!{{else}}NOT DEVOURED!{{/if}}
	</button>
</li> --}}

<li>
	<div class="col s6">
		{{burger_name}}
	</div>
	<div class="col s5">
		<button class="change-devour" data-id="{{id}}" data-newDevour="{{devoured}}">
	{{#if devoured}}PREP FOR EATING{{else}}DEVOUR!{{/if}}
		</button>
	</div>
</li>